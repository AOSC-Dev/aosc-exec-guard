# DEPRECATED — do not install this line.
#
# A catch-all entry (magic \x7fELF with mask \xff\xff\xff\xff) matches every
# exec, including the interpreter binary itself. The kernel then recurses
# between the file and the interpreter until it returns ELOOP, which breaks
# exec system-wide. See "踩坑记录" in README.md.
#
# Use the per-architecture entries in data/binfmt.d/ instead.
